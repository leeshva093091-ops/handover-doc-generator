# 개발 체크리스트 — 프로젝트 인수인계 문서 자동 생성기

> 기준 문서: [PRD.md](./PRD.md)
> 진행 순서: 0. 준비 → 1. MVP → 2. 다음 단계 → 3. 추후 → (전 단계 공통: 품질·문서)

---

## 0. 프로젝트 준비
- [x] 저장소/폴더 구조 잡기 (`handover_tool/handover`, `tests/`, `samples/`)
- [x] 파이썬 버전 고정 및 `README` 초안 작성 (Python 3.9+, `handover_tool/README.md`)
- [x] 의존성 정책 확정 — **MVP는 표준 라이브러리만** (폐쇄망 제약)
- [ ] 폐쇄망 패키지 반입 절차 확인 (`.whl` 사전 다운로드 경로) — MVP는 불필요, 웹 단계에서 필요
- [x] 인수인계 문서 **정형 템플릿 초안** 설계 (개요/설치/실행/환경/주의사항/구조 6섹션, `template.py`)

## 1. MVP — CLI + 정형 Markdown 출력
### 1-1. 소스 입력 (로컬 폴더)
- [x] CLI 인자 파싱 (`argparse`로 폴더 경로 입력 받기) — `cli.py`
- [x] 경로 유효성 검증 + 친절한 에러 메시지 (없는 경로/파일/권한, 종료코드 분리)

### 1-2. 자동 분석 (도구의 심장)
- [x] 디렉터리 구조 스캔 (`build_tree`, 잡음 디렉터리 제외)
- [x] README 탐지 및 핵심 내용 추출 (`find_readme`)
- [x] 의존성 파일 파싱 (`requirements.txt`·`package.json` 상세, 그 외 존재 표시)
- [x] 실행 스크립트/진입점 탐지 (`main.py`/`app.py`/`manage.py`·셸·Makefile·Docker·npm)
- [x] 언어/실행 진입점/포트 등 메타데이터 구조화 (`ProjectMetadata`)

### 1-3. 정형 양식 출력
- [x] 템플릿 슬롯에 메타데이터 매핑 (`render_markdown`)
- [x] 항상 동일한 섹션 구조로 Markdown 생성 (값 없어도 슬롯 유지 + `확인 필요`)
- [x] 결과 파일 저장 (`-o` 옵션, UTF-8)

### 1-4. MVP 마무리
- [x] 샘플 프로젝트 작성 (`samples/demo_project`) + 단위 테스트 작성 (`tests/test_analyzer.py`)
- [x] Python 3.13.14 설치 + 테스트 실행 확인 (7/7 통과)
- [x] 누락/오추출 케이스 점검 및 보정 — **cp949 콘솔 UnicodeEncodeError 버그 수정** (stdout/stderr UTF-8 강제)

## 2. 다음 단계 — 주의사항 추출 + 웹 화면
### 2-1. 주의사항 추출 ✅ (Python 검증 완료, 13/13 테스트 통과)
- [x] 환경변수 사용처 탐지 (`os.getenv`/`os.environ`/`process.env`/`System.getenv` → 환경 섹션)
- [x] 포트/외부 의존성 식별 (기존 포트 탐지 + 단일 스캔 루프로 통합)
- [x] 하드코딩된 키·접속정보 패턴 탐지 (`secrets.py`: 비밀값/접속문자열/AWS키/Bearer/개인키)
- [x] 민감정보 **마스킹 기본 적용** + "주의" 표에 위치(`파일:줄`) 표시
- [x] 오탐/누락 대비: 의심 항목 넓게 + 신뢰도(높음/중간/낮음), 자리표시자는 신뢰도 하향

### 2-2. 웹 화면 ✅ (실제 서버 GET/POST end-to-end 검증)
- [x] 웹 구동 방식 확정 — **표준 라이브러리 `http.server`** (외부 의존성 0 유지, `web.py`)
- [x] (Flask 미선택 → `.whl` 반입 불필요)
- [x] 입력 폼 (폴더 경로 / Git URL)
- [x] 분석 실행 → 결과 미리보기 화면 (textarea)
- [x] 민감정보 "주의" 경고 강조 표시
- [x] Markdown 다운로드 (data URI, 서버 왕복 없음)
- [x] 보안: 기본 127.0.0.1 바인드, 입력/에러 HTML 이스케이프(XSS 방지)

### 2-3. GitHub URL 입력 ✅ (실제 저장소 클론까지 검증)
- [x] URL 입력 → `git clone --depth 1` (폐쇄망 내부 GitHub 표준 경로, `source.py`)
      - git 없으면 추측 대신 명확한 안내 에러 / `--`로 인자 주입 차단 / 임시 클론 자동 정리
- [x] 로컬 폴더와 동일한 분석 파이프라인에 연결 (표시명·출처는 저장소명/URL로 표기)

## 3. 추후
- [ ] 다국어/다언어 프로젝트 폭넓게 지원
- [x] 같은 프로젝트 재분석 시 문서 갱신(diff) — `--snapshot` 플래그, `snapshot.py`
      - JSON 스냅샷 저장/비교, 의존성·환경변수·실행방법·포트·민감정보 추가/제거 표시
      - **새로 생긴 민감정보**는 별도 경고, 7번 섹션으로 출력 (실제 e2e 검증 완료)
- [ ] HTML 외 내보내기 포맷 확장

---

## 공통 — 품질 / 운영
- [x] 단위 테스트 (분석기·렌더링·빈 폴더 엣지케이스, `tests/test_analyzer.py`)
- [x] 에러 처리 및 엣지 케이스 (없는 경로/파일/권한, 빈 폴더, **인코딩(cp949) 문제** 해결)
- [x] 사용법 문서 — `README.md` + 설치 가이드 `INSTALL.md`(온라인/오프라인)
- [x] 비밀정보 하드코딩 금지 점검 (도구 자체에는 비밀정보 없음, stdlib만 사용)

---

## 성공 기준 (PRD 5섹션 연동)
- [ ] **온보딩 시간 측정**: 새 담당자가 생성된 문서만으로 설치·실행까지 도달 (목표 30분 이내)
